{
  "buildCommand": "pnpm install --frozen-lockfile && pnpm build",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": null,
  "projects": [
    {
      "name": "storefront",
      "root": "apps/storefront",
      "framework": "nextjs",
      "buildCommand": "pnpm --filter storefront build",
      "installCommand": "pnpm install --frozen-lockfile",
      "outputDirectory": "apps/storefront/.next",
      "devCommand": "pnpm --filter storefront dev"
    },
    {
      "name": "admin-dashboard",
      "root": "apps/admin-dashboard",
      "framework": "nextjs",
      "buildCommand": "pnpm --filter admin-dashboard build",
      "installCommand": "pnpm install --frozen-lockfile",
      "outputDirectory": "apps/admin-dashboard/.next",
      "devCommand": "pnpm --filter admin-dashboard dev"
    }
  ]
}
